import { DialogConfirmationTriggerDirective } from './dialog-confirmation-trigger.directive';

describe('DialogConfirmationTriggerDirective', () => {
  it('should create an instance', () => {
    const directive = new DialogConfirmationTriggerDirective();
    expect(directive).toBeTruthy();
  });
});
